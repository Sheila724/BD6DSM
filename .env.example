# =====================================================
# Banco PRIMARIO (escrita) — VPS ou ambiente do professor
# =====================================================
DB_PRIMARY_HOST=SEU_IP_OU_DOMINIO_VPS
DB_PRIMARY_PORT=3306
DB_PRIMARY_DATABASE=aula-db
DB_PRIMARY_USERNAME=bd6dsm
DB_PRIMARY_PASSWORD=SUA_SENHA

# =====================================================
# Replicas (leitura) — host:porta separados por virgula
#
# Desenvolvimento na VPS (sem replicacao real):
#   aponte escrita e leitura para o MESMO host
#   DB_REPLICAS=SEU_IP_OU_DOMINIO_VPS:3306
#
# Apresentacao (professor fornece IPs na hora):
#   DB_REPLICAS=IP_REPLICA_1:3306,IP_REPLICA_2:3306
# =====================================================
DB_REPLICAS=SEU_IP_OU_DOMINIO_VPS:3306
DB_REPLICA_DATABASE=aula-db
DB_REPLICA_USERNAME=bd6dsm
DB_REPLICA_PASSWORD=SUA_SENHA

# =====================================================
# Servidor
# =====================================================
PORT=8080
